export interface Slim2Country {
        name : string;
        'alpha-2' : string;
        'country-code' : string;
}

export interface Country {
        isoCode: string;
        name: string;
        flagLink: string;
}
